GOAL: Produce a written inventory of the current codebase so the following change prompts can be adjusted to reality. Make NO code changes in this step. Do not edit, rename, or create any file. Output only a report.

Work through each section below and report exactly what you find. Where something is missing, say "NOT FOUND" — that is a valid and useful answer.

=== SECTION A — Project shape ===
A1. List the top-level structure of src/ (screens, components, data, lib).
A2. Confirm these files exist and give their paths; flag any that are missing or renamed:
    - src/data/yard-data.ts
    - src/data/yard-ops.ts
    - src/lib/yard-layout.ts
    - src/lib/backend-adapters.ts
    - src/lib/utils.ts
    - src/screens/NightPlanner.tsx
    - src/screens/GateConsole.tsx
    - src/screens/YardMap.tsx
    - src/screens/ControlTower.tsx
    - src/components/yard/SlotStackView.tsx
    - src/App.tsx

=== SECTION B — Zones (drives prompts 1.1 and 2.1) ===
B1. In src/data/yard-data.ts, find the Zone interface. List every field and its type.
B2. Find the ZONES array. For each zone, report its `id` and current `name` verbatim.
B3. Report whether any zone already has `reefer`, `quarantine`, or a Reefer/Quarantine name. (Tells us if 2.1 is already partly done.)
B4. In src/lib/yard-layout.ts, find ZONE_LAYOUT (or equivalent). List which zone ids have layout coordinate entries. Note the shape of each entry (e.g. {x, y, cols}). Report the highest x/y used so we know where free canvas space is for new zones.
B5. Confirm whether zone ids are used as lookup keys (e.g. ZONE_LAYOUT[zone.id]) and whether container addresses embed the zone id (e.g. "A-02-1-3-2"). Quote one real address from the data.

=== SECTION C — Move types (drives prompt 1.2) ===
C1. In src/data/yard-data.ts, find MOVE_TYPES and TYPE_LABEL. Report every key and its current display label verbatim.
C2. Search all of src/ for the display strings "Reshuffle", "Place inbound", "Retrieve to staging", "Load outbound", "Receive from lane". List every file + line, and for each, state whether the string is a KEY/identifier (ALL_CAPS or used in a filter/lookup) or a LABEL (Title Case, shown to the user).
C3. In src/lib/backend-adapters.ts, report any move-type label mappings (e.g. shuffle: "Reshuffle").

=== SECTION D — Terminology counts (drives prompt 1.3) ===
D1. Count occurrences of "jockey" and "operator" across src/. List the files containing each.
D2. Report whether the term "PIFO" already appears anywhere.
D3. Report whether "Gate-in", "Gate-out", "ASN", "EDI 322", "CEDEX" already appear anywhere.
D4. In src/screens/YardMap.tsx and SlotStackView.tsx, find where the placement reason (`whyHere` or similar) is displayed. Report the surrounding JSX so we know where to add the PIFO caption.

=== SECTION E — Container model (drives prompt 2.2) ===
E1. In src/data/yard-data.ts, find the Container interface. List every field and type.
E2. Report whether any of these already exist: isoType, reefer, tempSetPoint, tareKg, damageCode, hold, highValue, ageDays.
E3. Find the function that generates containers (e.g. buildContainers). Briefly describe how it assigns zone, address, size, weight — enough to know how to populate new fields.
E4. Report the exact address string format and the ORDER of its segments (zone-block-row-slot-tier? or another order?). Quote the parsing/formatting code if present. THIS IS CRITICAL for the placement-rule parser in 2.3.

=== SECTION F — Placement / move display (drives prompts 2.3 and 2.4) ===
F1. Report whether a file like src/lib/placement-rules.ts already exists, and whether any function like checkPlacementRules already exists.
F2. In NightPlanner.tsx and SlotStackView.tsx, find where a move's reason/detail is rendered. Report the JSX location where a rule-block message could be surfaced.
F3. Confirm the Container/move fields available at that render point (weight, size, address of destination, what's below it) — we need these to evaluate no-crush, tier, and size rules.

=== SECTION G — Gate console (drives prompt 2.5) ===
G1. In GateConsole.tsx, find the EIR photos accordion. Report the state variable name, the accordion header/body pattern, and how open/close is toggled — we will mirror this for an Inspection accordion.
G2. Report the selected-visit object and which fields it exposes (container, driver, seal, etc.).
G3. Report whether any inspection/CEDEX/seal-validation UI already exists.

=== SECTION H — Navigation (drives prompt 2.6) ===
H1. In src/App.tsx, report how screens are registered (the Screen type, the nav list, BADGE_COUNT if present).
H2. Report whether existing screens use an internal tab pattern (so we can add the Inventory view as a tab rather than a new nav item). Point to one example of a tabbed screen if one exists.

=== SECTION I — Demo story (drives prompt 2.7) ===
I1. In src/App.tsx, find the STORY array (or equivalent). Report each step's screen target and its title/caption text verbatim.

=== OUTPUT ===
Produce the report as a single structured document with the section letters above. At the end, add a "DELTA SUMMARY" listing:
  - Which of the planned changes appear to be ALREADY DONE (skip those prompts).
  - Which planned file paths or field names DIFFER from what was found (substitute the real ones).
  - Any NOT FOUND items that mean a prompt needs rethinking.
Do not proceed to any change. Wait for the go-ahead.